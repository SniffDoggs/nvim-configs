#NVIM-CONFIGS

A repo to store my neovim config files. Purely vibe-coded.

**NOTE:** Tree-sitter auto-install is broken. Highlighting for keywords like 'struct', 'typedef', and 'enum' have been forcefully injected firectly via a 'ColorScheme' autocmd override.

A customized fork of [Magicalbat's repo](https://github.com/Magicalbat/Configs). The only human contribution is removing and replacing the corny AI-made comments in the code.
